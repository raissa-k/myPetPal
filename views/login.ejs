<%- include("partials/header", {title: "Login" }); %>

<body class="text-center puppy-paw-background">
    <% if (locals.messages.errors) { %>
            <% messages.errors.forEach( el=> { %>
                <div role="alert" class="alert alert-danger">
                    <%= el.msg %>
                </div>
                <% }) %>
    <% } %>

    <% if (locals.messages.info) { %>
            <% messages.info.forEach( el=> { %>
                <div role="alert" class="alert alert-warning">
                    <%= el.msg %>
                </div>
            <% }) %>
    <% } %>

    <main class="w-100 m-auto">
        <form action="/login" method="POST" class="mb-4">
            <img class="mb-3" src="https://www.svgrepo.com/show/177658/clipboard-list.svg"
                alt="Clipboard with a lock" width="50" height="50">
            <h1 class="display-4 text-success mb-5 fw-normal">Log in</h1>
            <div>
                <input type="email" name="email" class="form-control" id="floatingEmail"
                    placeholder="Email">
                <label for="floatingEmail" class="mb-0">Email</label>
            </div>
            <div>
                <input type="password" class="form-control" id="floatingPassword" name="password"
                    placeholder="Password">
                <label for="floatingPassword" class="mb-0">Password</label>
                <input type="submit" class="w-100 btn btn-lg btn-success">
            </div>
        </form>
        <a href="/signup" class="btn btn-outline-success g-2 px-4">Or Sign Up</a>
    </main>
<style>
    body {
        display: flex;
        align-items: center;
        padding-top: 40px;
        padding-bottom: 40px;
    }

    main {
        max-width: 330px;
        padding: 15px;
    }

    .alert {
        position: absolute;
    }

    form label {
        visibility: hidden
    }
</style>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8" crossorigin="anonymous"></script>
</body>

</html>